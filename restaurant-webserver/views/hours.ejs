<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/style.css">
    <title>Working Hours | Restaurant</title>
  </head>
  <body>
    <h1>Our Hours</h1>
    <% for (let day of days) { %>
    <% const hoursObj = workingHours.hasOwnProperty(day) ? workingHours[day] : workingHours["default"] %>
    <section style="display: inline-flex; flex-direction: column; padding: 1em">
      <h2><%= day.toUpperCase()%></h2>
      <div>
        <% if (hoursObj) { %>
        <p>Open: <%= hoursObj.open %></p>
        <p>Closed: <%= hoursObj.closed %></p>
        <% } else { %> CLOSED <% } %>
      </div>
    </section>
    <% } %>
  </body>
</html>
